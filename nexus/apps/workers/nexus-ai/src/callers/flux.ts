// FLUX.1 Pro image generation caller (via fal.ai)
// Full implementation in Task 3

export async function callFlux(
  _apiKey: string,
  _prompt: string
): Promise<{ url: string }> {
  // Placeholder
  return { url: "" };
}
