// DeepSeek AI caller (DeepSeek-V3, DeepSeek-R1, DeepSeek-Coder-V3)
// Full implementation in Task 3

export async function callDeepSeek(
  _model: string,
  _apiKey: string,
  _prompt: string
): Promise<{ text: string; tokens?: number }> {
  // Placeholder
  return { text: "", tokens: 0 };
}
