// Qwen AI caller (Qwen 3.5 Flash, Qwen 3.5 Max via SiliconFlow)
// Full implementation in Task 3

export async function callQwen(
  _model: string,
  _apiKey: string,
  _prompt: string
): Promise<{ text: string; tokens?: number }> {
  // Placeholder
  return { text: "", tokens: 0 };
}
