name = "nexus-workflow"
main = "src/index.ts"
compatibility_date = "2024-12-30"

# Service Bindings (free internal routing)
[[services]]
binding = "NEXUS_AI"
service = "nexus-ai"

[[services]]
binding = "NEXUS_STORAGE"
service = "nexus-storage"

[[services]]
binding = "NEXUS_VARIATION"
service = "nexus-variation"

# D1, KV, R2, CF Workflows Durable Objects — configured at deploy time (Task 16)
