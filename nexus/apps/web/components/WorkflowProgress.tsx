// Workflow Progress — shows step-by-step progress with AI model + cost tracking
export default function WorkflowProgress() {
  return (
    <div>
      <h3 className="text-lg font-semibold mb-2">Workflow Steps</h3>
      <p className="text-gray-500">Step progress will be implemented in a later task.</p>
    </div>
  );
}
